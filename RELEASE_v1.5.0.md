# Orpheus TTS v1.5.0 Release Notes

## 🚀 发布信息

**版本**: v1.5.0  
**发布日期**: 2025-12-14  
**Git Tag**: `v1.5.0-bfloat16-3b-preload`  
**Docker Tag**: `neosun/orpheus-tts:v1.5.0-bfloat16-3b-preload`  
**Digest**: `sha256:e23100eb66329ca3a6c2c63d5ec11194e429592727015eba78ee2f68ca102950`

## ✨ 新功能

### 1. 🚀 模型预加载
- **启动时自动加载模型到显存**
- 首次请求无需等待模型加载
- 从 48秒 降至 1.8秒（**26倍提速**）

### 2. 🎤 零样本语音克隆 UI
- 添加自定义语音选项
- 支持上传参考音频文件
- 3-10秒清晰语音样本即可克隆
- UI 自动显示/隐藏文件上传区域

### 3. ⏱️ 生成计时显示
- 实时显示三个关键指标：
  - **模型加载时间**（预加载后为 0.000s）
  - **音频生成时间**（~1.8s）
  - **总耗时**（~1.8s）
- 美观的卡片式展示
- 不同颜色区分不同指标

## 📊 性能对比

| 指标 | v1.0.0 | v1.5.0 | 提升 |
|------|--------|--------|------|
| 首次请求 | 48.0s | 1.8s | **26x** |
| 模型加载 | 15.0s | 0.0s | **∞** |
| 音频生成 | 2.5s | 1.8s | **28%** |
| 显存占用 | 39GB | 30GB | 节省 23% |

## 🎯 技术改进

### 模型预加载机制
```python
def preload_models():
    """启动时预加载默认模型到显存"""
    print("🚀 Preloading model to GPU...")
    start_time = time.time()
    try:
        model = gpu_manager.get_model("medium-3b", lambda: load_model("medium-3b"))
        elapsed = time.time() - start_time
        print(f"✅ Model preloaded in {elapsed:.2f}s")
        return True
    except Exception as e:
        print(f"⚠️  Model preload failed: {e}")
        return False
```

### API 响应头增强
```
X-Model-Load-Time: 0.000
X-Generation-Time: 1.801
X-Total-Time: 1.801
```

### UI 改进
- 添加语音克隆文件上传控件
- 动态显示/隐藏上传区域
- 实时计时信息展示
- 三色卡片式指标显示

## 🐳 Docker 镜像

### 镜像标签
```bash
neosun/orpheus-tts:v1.5.0-bfloat16-3b-preload  # 完整标签
neosun/orpheus-tts:v1.5.0-allinone             # 简短标签
neosun/orpheus-tts:latest                       # 最新版本
```

### 使用方法
```bash
# 拉取镜像
docker pull neosun/orpheus-tts:v1.5.0-allinone

# 运行容器（模型会自动预加载）
docker run -d \
  --name orpheus-tts \
  --gpus '"device=3"' \
  -p 8899:8899 \
  -e HF_TOKEN=your_token \
  neosun/orpheus-tts:v1.5.0-allinone

# 等待模型预加载完成（约30秒）
sleep 30

# 测试（应该在2秒内完成）
curl -X POST http://localhost:8899/api/generate \
  -H "Content-Type: application/json" \
  -d '{"text":"Hello world","voice":"tara","model_size":"medium"}' \
  --output test.wav
```

## 🔄 从 v1.0.0 升级

### 1. 停止旧容器
```bash
docker stop orpheus-tts
docker rm orpheus-tts
```

### 2. 拉取新镜像
```bash
docker pull neosun/orpheus-tts:v1.5.0-allinone
```

### 3. 启动新容器
```bash
docker run -d \
  --name orpheus-tts \
  --gpus '"device=3"' \
  -p 8899:8899 \
  -e HF_TOKEN=$HF_TOKEN \
  -e GPU_IDLE_TIMEOUT=3600 \
  neosun/orpheus-tts:v1.5.0-allinone
```

### 4. 验证
```bash
# 检查健康状态（模型应该已加载）
curl http://localhost:8899/health

# 应该看到：
# "loaded_models": ["medium-3b"]
# "gpu_memory": ~30GB
```

## 🎨 UI 新特性

### 语音克隆界面
1. 选择 "🎤 自定义语音克隆"
2. 上传 3-10秒 的参考音频
3. 输入文本
4. 点击生成

### 计时显示
生成完成后自动显示：
- 🔵 模型加载时间（蓝色）
- 🟣 音频生成时间（紫色）
- 🟢 总耗时（绿色）

## 🐛 已知问题

### 语音克隆功能
- ⚠️ 当前版本 UI 已添加，但后端实现待完善
- 选择自定义语音会回退到默认语音
- 计划在 v1.6 中完整实现

### 显存占用
- 预加载后显存占用 ~30GB
- 比 v1.0 的 39GB 降低了 23%
- 仍需 40GB+ 显存的 GPU

## 📝 更新日志

### v1.5.0 (2025-12-14)
- ✅ 添加模型预加载功能
- ✅ 添加零样本语音克隆 UI
- ✅ 添加生成计时显示
- ✅ 优化性能：首次请求 26x 提速
- ✅ 优化显存：降低 23% 占用
- ✅ 更新 Dockerfile 支持预加载
- ✅ API 响应添加计时头

## 🔜 下一步计划

### v2.0.0 - AWQ 4-bit 量化版本
- 模型：Hariprasath28/orpheus-3b-4bit-AWQ
- 预计显存：~10-15GB
- 预计性能：与 bfloat16 相当或更快
- 状态：准备开发

### v1.6.0 - 功能增强
- 完整实现零样本语音克隆
- 添加多模型切换支持
- 实现请求队列管理
- 添加监控和日志系统

## 🙏 致谢

感谢社区反馈和建议，帮助我们持续改进 Orpheus TTS！

---

**GitHub**: https://github.com/neosun100/orpheus-tts-docker  
**Docker Hub**: https://hub.docker.com/r/neosun/orpheus-tts  
**模型**: https://huggingface.co/canopylabs/orpheus-3b-0.1-ft
